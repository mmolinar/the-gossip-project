<div class="cover-container d-flex w-100 p-3 mx-auto flex-column">
  <header class="masthead mb-auto">
    <div class="inner">
        <nav class="nav nav-masthead">
          <p class="navbar-brand"> <%= link_to "Home", "/"%> </p>     
          <p class="nav-link active"> <%= link_to "Index", "/index"%> </p>
          <p class="nav-link active"> <%= link_to "Team", "/team"%> </p>
          <p class="nav-link active"> <%= link_to "Contact", "/contact"%> </p>
          <p class="nav-link active"> <%= link_to "New gossip", "/gossips/new"%> </p>
          <% if !current_user %>
            <p class="nav-link active"> <%= link_to "Login", login_path%> </p>
            <p class="nav-link active"> <%= link_to "Signup", new_user_path%> </p>
          <% end %>
          <% if current_user %>
            <p class="nav-link active"> <%= link_to "Logout", session_path(session.id), method: :delete%> </p>
          <% end %>
        </nav>
    </div>
  </header>
</div>